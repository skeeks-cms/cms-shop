# GPD: приём журнала каталога на клиенте

Текущая схема и правила: [GPD v2](gpd.md). Разделы с датами — история внедрения.

Текущее применение карточек и компонент настроек описаны в [gpd-catalog-application.md](gpd-catalog-application.md). Ниже также сохранена история этапа приёма без применения.

Первый клиентский этап — приём метаданных без изменения каталога магазина.
`shop.gpd.catalog.receive` получает manifest/bootstrap, затем ленту изменений и
проверяет pending через batch. Он НЕ импортирует карточки, не меняет цены/остатки,
не деактивирует и не удаляет товары. Старый SkeeksSuppliersController не изменён.
Следующий этап — отдельный применяющий обработчик и правила владения полями.

## Состояние

- `shop_gpd_connection`: ID подключения, сайт, адрес v2, отпечаток ключа,
  фаза, курсор, граница ленты, поколение и счётчики первоначальной выгрузки.
- `shop_gpd_catalog_state`: одна строка на подключение/товар — последняя полученная
  ревизия, ревизия карточки, операция, необходимость уточнения и поколение manifest.
  JSON и ключ API здесь не хранятся. Это состояние протокола, не очередь выполнения.
- Записи страницы и курсор фиксируются одной транзакцией под блокировкой подключения.
  Сбой до commit оставляет старый курсор; повтор безопасен. Параллельный ответ со
  старым курсором отклоняется. Работа с HTTP выполняется вне транзакции.
- `revision` — порядок состояния товара, `position` — позиция события в ленте.
  Позднее событие может возвращать более новую текущую карточку. Эти числа нельзя
  смешивать; курсор принимается как непрозрачная строка от API.
- pending сохраняется явно. Отсутствие в законченной выгрузке означает необходимость
  batch-проверки, а не разрешение удалить товар. `not_available` без ревизии также
  не считается revoke. Просроченный курсор запускает новый bootstrap, сохраняя
  прежние состояния до проверки полного manifest. 401/403/503/invalid_cursor ничего
  не очищают. Смена ключа, сайта или URL блокируется как смена идентичности; для
  ротации нужен отдельный проверенный перенос подключения, автоматического reset нет.

## Native job и opt-in

Регистрация типа и очереди `gpd-receive` сама по себе ничего не запускает.
Расписания и подключения по умолчанию отсутствуют. Перед пилотом:

1. Установить код cms-shop и применить его миграцию
   `m260917_030000_gpd_catalog_receiver` штатным cms/migrate.
2. На сервере API отдельно разрешить v2 после проверки готовности журнала.
3. В конфигурации КЛИЕНТСКОГО сайта добавить компоненту gpdReceiver:

```php
'enabled' => true,
'siteId' => 1, // фактический cms_site.id, не копировать без проверки
'url' => 'https://gpd-api.skeeks.com/v2',
'apiComponent' => 'skeeksSuppliersApi',
```

Ключ берётся из указанного существующего компонента и не помещается в job payload.
ID подключения глобально уникален в установке и не переиспользуется для другого API.
В одной БД можно принимать метаданные разных подключений; это ещё не гарантирует
поддержку нескольких источников существующим импортом карточек.

4. Развернуть отдельный worker `yii cms-job/worker --queue=gpd-receive`.
5. Создать расписание с job_type `shop.gpd.catalog.receive`, payload
   `{}`, правильным сайтом и интервалом 60 секунд. Сначала
   проверить ручной запуск. Нынешние четыре команды не переключаются автоматически.

Ресурс и dedup относятся к приёму каталога установки; overlap=skip, до трёх попыток при сбое.
Обработчик использует ChunkedJobHandler, страницы по 50, batch по 20, перезапуск
процесса примерно через 15 секунд. Нет собственного worker, claim или retry loop.
Каждый запуск один раз проходит unresolved; если они остались, результат — с
предупреждением и следующим штатным запуском. Перед переключением на применение
надо убрать параллельных старых писателей карточек, но при проверке только приёма
они могут продолжать работать.

## Проверки и границы

`tests/gpd-catalog-receiver.php`: отдельная MariaDB 10.5, префиксы пустой и sx_,
10 000 товаров через настоящий ChunkedJobHandler, принудительные продолжения,
отмена, rollback страницы, ревизии, восстановление, изоляция подключений и прогресс.
HTTP и содержимое API в этой проверке задаёт транспортная фикстура; это не пилот
на рабочем клиенте и не проверка импорта CMS-моделей. TLS transport запрещает
redirect и не включает ключи/ответы сервера в исключения.

API ограничивает активные bootstrap-сессии: потеря ответа на создание сессии может
привести к 429 до её истечения. Не обходить лимит бесконечным bootstrap. Периодический
job повторит запрос; идемпотентное создание сессий — отдельное улучшение протокола.
Полные свойства, очистка картинок/коллекций, справочники, локальные правки,
деактивация, ACK применения, offers и переключение старых расписаний ещё впереди.

## Пилот elitkras.ru, 17.09.2026

Развёрнут приёмник на s1/CT104, /var/www/sites/elitkras.ru, cms_site=1.
Установлены файлы receiver из 4dd315d и отдельная миграция (0.107s).
Worker skeeks-site-1345-gpd-receive.service active/enabled, очередь gpd-receive.
Расписание #60: shop.gpd.catalog.receive, connection=elitkras, интервал60s.
Старые расписания #55–58 оставлены активными.

На стороне GPD разрешён v2 только клиенту351. Используется прежний API-ключ;
новый серверный ключ подписи курсоров не является клиентским ключом.
Первый запуск #16 выявил ошибку реального HTTP: GET cursor/limit уходили в JSON
вместо query string. Исправлено в CatalogTransport, добавлены 10 wire-тестов с
настоящим форматированием yii/httpclient. Новый запуск #18 продолжил ту же
сохранённую сессию и завершился succeeded:597/597, unresolved0, ошибок0.
SHA256 отсортированных ID/revision/product_revision/operation совпал с сервером:
9f453163fc5581289c1f309e1053ab1a9d2a0ba331792117e1803302e512bde8.
Это подтверждение приёма метаданных, не изменения 597 карточек магазина.

Диагностика /tmp/elitkras-gpd/status.php и verify-client.php, резерв до установки
/tmp/elitkras-gpd/backup-code.tar.gz (root-only). Применение карточек/деактивация
по-прежнему не реализованы и не включены; четыре прежних обмена продолжаются.

## Понятный результат каждого запуска

Обработчик сохраняет run.cards_received, exclusions_received, pending_received,
rechecked отдельно от общего состояния597/597. Эти счётчики продолжаются между
порциями одного run и начинаются с нуля у следующего. Завершение выставляет
progress_message через setStage('complete', ...), как мониторинг hosting.sites.monitor.
При пустой ленте: «Новых изменений нет. Требуют проверки: 0. Товары сайта не изменялись.»
Проверено85 сценариев получателя, включая10тыс, продолжение счётчиков, новый пустой
запуск и отдельный revoke. На elitkras.ru новый run33 succeeded с таким сообщением.
Список запусков cms-job теперь предпочитает progress_message техническому stage;
старый stage остаётся запасным вариантом, HTML сообщения экранируется.

## Запуск без параметров подключения

Задание принимает пустой payload {} и выбирает настройку по cms_site_id запуска.
Публичное имя connection больше не требуется: одно подключение на сайт.
ReceiverComponent имеет плоские enabled/siteId/url/apiComponent. Внутренний ID
строки состояния сохраняется при переходе со старой конфигурации, поэтому
переименование настроек не сбрасывает курсор. Для новой установки ID формируется
автоматически как site-N. Неоднозначная конфигурация отклоняется; смена ключа
по-прежнему не подменяет прежнюю товарную базу молча. Старые connections и payload
принимаются для совместимости уже поставленных заданий, с проверкой сайта.
Старый и пустой payload используют один ресурсный ключ.

Пилот: расписание60 теперь {}, интервал60s; новый запуск70 succeeded.
Перед изменением проверено сохранение всей строки состояния, tracked597.
94 проверки, включая сохранённый курсор, неверный сайт, смену ключа,
неоднозначность и настоящий job без параметров на10000 товаров.
Новые применяющие jobs разрабатываются отдельно; старые команды остаются
справочником бизнес-правил, а не исполняемым адаптером новых jobs.

After receipt/resolve commits new metadata, CatalogApplyDispatch checks for
unapplied resolved rows scoped through the site's connection and pushes the
native shop.gpd.catalog.apply job with an empty payload. It respects the GPD
component switch and existing job resource/dedup policy. Each bounded receiver
execution wakes once on its first nonempty page and checks again on finish,
including empty polls with pending application. Regular apply schedules remain
the recovery path if enqueue fails or overlaps an already running writer.
The receiver owns no source JSON generation or publisher-specific bootstrap.
